import React from "react";
import ReactDOM from "react-dom/client";

function App() {
  return (
    <div style={{ padding: 16 }}>
      <h1>BR Pos</h1>
      <p>Base inicial: UI + build + releases</p>
    </div>
  );
}

ReactDOM.createRoot(document.getElementById("root")!).render(<App />);